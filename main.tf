terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 4.16"
    }
  }

  required_version = ">= 1.2.0"
}

provider "aws" {
  region  = "us-east-1"
}

output "webpage-dns" {
  value = aws_cloudfront_distribution.webpage_cf.domain_name
}
